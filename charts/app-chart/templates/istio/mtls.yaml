{{- if and .Values.istio.enabled .Values.istio.peerAuthentication.enabled }}
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: {{ include "sample.releaseName" . }}
spec:
  mtls:
    mode: STRICT
{{- end }}
