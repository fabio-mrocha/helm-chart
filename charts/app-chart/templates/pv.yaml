{{- if .Values.persistentVolume.enabled }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ include "sample.releaseName" . }}
{{- if .Values.persistentVolume.spec }}
spec:
  {{- toYaml .Values.persistentVolume.spec | nindent 2 }}
{{- end }}
{{- end }}
